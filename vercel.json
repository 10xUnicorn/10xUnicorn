{
  "buildCommand": "cd frontend && npm install --legacy-peer-deps && npx expo export --platform web",
  "outputDirectory": "frontend/dist",
  "framework": null,
  "installCommand": "echo skip",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
